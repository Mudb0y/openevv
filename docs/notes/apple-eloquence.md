# Measured against Apple's Eloquence

Apple ships ETI Eloquence as the voice behind VoiceOver, in `eci.dylib` inside `TextToSpeechKonaSupport.framework`. It reports itself as ECI 6.1. So there is a second, independent build of this engine in the world, and in September 2026 it was measured against ours.

**Our 11.025 kHz output is Apple's.** Two utterances, both captured engine-native with no rate conversion anywhere in the path, on Apple's Reed preset -- which is voice 1, and its eight parameters are ours exactly: head size 50, pitch 65, fluctuation 30, roughness 0, breathiness 0, speed 50, volume 92.

A sustained vowel, in 500 Hz bands from nought to 5.5 kHz, dB relative to each file's own strongest band, ours against theirs: 0.00/0.00, -4.15/-3.89, -9.31/-9.16, -28.06/-27.90, -19.32/-19.30, -19.24/-19.26, -25.32/-25.32, -21.64/-21.67, -35.75/-35.80, -52.55/-52.50, -58.65/-58.54. Agreement to a tenth of a decibel.

And four sentences chosen for sibilants, plosives and transitions, in 250 Hz bands over the same span: through 3,250 to 4,250 Hz, where the engine's response is flat, the two agree to within **0.07 dB**. The largest disagreement anywhere is 0.76 dB, and it sits at 4,750 to 5,000.

Which is the benign place for it, and worth saying so the bound is not read as the result. The level falls 5.4 dB across 4,250 to 5,000, and on a slope that steep a difference of a few tens of hertz in where the fall begins reads as most of a decibel. So the disagreement tracks the derivative and not the level: it is near nothing wherever the response is flat and appears only on the rolloff. The concerning shape would have been that same figure in the middle of the spectrum where the response is level, which is a different fault entirely.

That is a stronger statement than `test/suite.sh` can make. The suite holds us against the very objects we were transcribed from, so it proves the transcription and not the model. This holds us against a different implementation, on a different architecture, compiled by a different vendor from a different source tree.

What it does not cover, and the limits are worth stating because the two engines are now known to be separately implemented -- which is exactly where separate implementations would be most likely to diverge. One voice of eight. One language of nine. The steady-state spectrum and the sibilant and transition bands, not prosody, timing or marks. Widening it needs nothing but more captures of the same kind: the method is in the last section here.

**It is a different implementation, and that matters.** `KlattSynth` in Apple's language module is floating point: 117 SSE instructions in its first two hundred lines and not one fixed-point multiply. Ours is entirely fixed point -- `imul` and a shift by fifteen, Q15 tables, no float in the signal path. `KlattSetConstParms` differs too: Apple's copies 88 bytes to state offset 0xe40 where ours copies 68 to 0xa80, and its next instruction reads the sample rate as a **float** and divides by it, where ours indexes precomputed tables. ECI 6.1's Klatt and IBM's ViaVoice Klatt are two implementations of one model, not two versions of one program.

So agreement at 11.025 kHz says the model is right in both. It does not license any expectation about behaviour at a rate neither vendor tuned for, and that is the trap this investigation fell into.

**Apple's high quality mode, and why nobody can find the switch.** VoiceOver can put the engine at 16 kHz. It is not the `eciSampleRate` parameter, which in Apple's build accepts 0 and 1 and rejects everything else. It is `eciRegisterKlattHooks2`: the caller is handed the synthesiser's own constant-parameter block before it is used, and the sample rate is a field in that block, so Apple's wrapper rewrites it. `-[AXKonaSpeechEngine setHighQualityMode:]` is the boolean that turns it on, it has no preferences key anywhere in the image, and the hook address reaches the language module as data rather than as a linked symbol. There is nothing to find from outside, which is why people looking for a sample-rate setting never find one.

Two more numbers from that path, established by sweeping to byte identity against two unrelated utterances: the high-formant frequency and bandwidth scaling factors default to exactly 2/3 and 5/6, and both are wholly inert at 11.025 kHz and live only at 16.

**What our own 16 kHz does, and why it is not a defect.** Synthesised at 16,000 our engine loses up to 24 dB through 3 to 5.5 kHz relative to its own 11.025 kHz output, where Apple's gains up to 8.8. Isolated from the engine entirely -- an impulse through `pole_filter`, no synthesiser around it -- one resonator at 750 Hz with 120 bandwidth loses 3.93 dB at 5,250 Hz across that rate change, and the vowel's five cascade formants in series lose 25.88. Measured through the whole engine: 23.86. The same number.

The reason is in the design rather than the port. A two-pole all-pole resonator has no zeros, so its response at Nyquist is pinned by its own coefficients at `A / (1 + b + r^2)`. Hold a formant at a fixed frequency and bandwidth in hertz and raise the rate: the DC-normalising gain `A` falls as roughly one over the rate squared while the denominator barely moves, so the floor the skirt lands on drops. For that formant it is -26.5 dB at 11.025 kHz and -33.1 at 16. Five in cascade multiply it.

Everything reachable was tried against Apple's measured vowel spectrum and none of it closes the gap: enabling F6 to F8 in the cascade at every count from five to eight (15 to 19 dB hot, because three resonators at 5,000, 6,300 and 7,500 with IBM's bandwidths add 12 dB at 3 kHz and 17 at 3.5), their parallel counterparts (A6F, A7F and A8F read zero on every frame and the frication amplitude is zero throughout a vowel, so that branch cannot contribute), widening those bandwidths from 500 to 4,000 (all worse than not having them), and widening the whole cascade to lift the skirts directly (improves the residual spread from 10.7 dB to 8.7 and then plateaus).

So 16 kHz synthesis stays behind `EVV_UPSAMPLE=none` rather than becoming a setting. It is the correct fixed-point Klatt at that rate and it is duller than the same engine at 11.025 through the consonant band. Matching Apple there would mean porting ECI 6.1's floating-point Klatt, which is a second synthesiser from a different codebase and not a tuning exercise.

**How it was measured.** Apple's engine runs on Linux: `~/Projects/apple-eloquence-elf` converts the Mach-O dylibs to ELF, and `examples/speak.c` there is the shape of a driver. The x86_64 slice comes out with `llvm-lipo -thin`, the conversion wants only `python3` with `lief`, and the link wants `libc++` on `LIBRARY_PATH`. Nothing needs installing and nothing needs a Mac. `tools/measure/rates.py` reads a band against its own reflection, which is what separates a resampler's images from content the source never had.

One trap worth carrying: `say -o` on macOS is **not** the engine's output. CoreAudio's default converter takes 12.4 dB out of the band from 5,016 to 5,512 hertz, which reads exactly like a deliberate anti-alias filter in the engine and is not one. A capture is only the engine's if nothing resampled it, or if the converter was explicitly set to its mastering quality.
