通常の音声 `vs50 今はずっと遅くなりました。
音量 `vv30 小さく、それから `vv90 また大きく。
高さ `vb20 低く、`vb80 高く、一つの文の中で。
声 `v2 二番、`v5 五番、そして `v1 一番に戻る。
短い休止 `0 と長い休止 `4 が言葉の間に。
かすれ `vr40 と息 `vf60 を変えました。
文字としての \`逆引用符は読み上げられます。
速さを名前で `vsfast そしてまた `vsslow。
頭の大きさ `vh30 それから `vh70 同じ行で。
性別 `vg1 と `vg0 が交互に。
印の前 `ui"mark one" と印の後ろ。
音の印 `aud"beep" が行の途中に。
`vs40 いちばん初めに置いた注釈です。
行の終わりで終わる注釈 `vs60
壊れた `vzz と `q と `v999 が一行に。
二つの `vs30`vv40 注釈が間を置かずに。
`ui"a"`ui"b" 二つの印が間を置かずに。
印のすぐ後に句点 `ui"mark two"。
一緒に `v3 声 `vs70 速さ `vv50 音量。
タブ	と改行が `vb40 変化とともに。
